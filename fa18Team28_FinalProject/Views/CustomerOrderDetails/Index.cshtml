@model IEnumerable<fa18Team28_FinalProject.Models.CustomerOrderDetail>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExtendedPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtendedPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomerOrderDetailID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomerOrderDetailID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomerOrderDetailID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
